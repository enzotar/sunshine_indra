use std::{io::BufReader, path::Path};

// use sonnerie::CreateTx;

// initiate a transaction file

// fn create_transaction() -> sonnerie::CreateTx {
//     let path = Path::new("./database/");
//     sonnerie::CreateTx::new(&path).unwrap()
// }

// pub fn initialize() {
//     let transaction = create_transaction();
//     let format = "u";
//     let timestamp_format = std::option::Option::Some("%FT%T");
//     let input = "123";
//     let f = BufReader::new();

//     sonnerie::formatted::add_from_stream(&mut transaction, format, input, timestamp_format);
// }
